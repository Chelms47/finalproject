var email = prompt("Please enter your email ro recieve newsletter", );

if (email == null || email == "") {
    txt = "No Thank you";
} else {
    txt = "newsletters will be sent to " + person + "! Thanks for staying in touch";
}